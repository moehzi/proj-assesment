import React from 'react';
import FormInput from '../../components/FormInput';
import Header from '../../components/Header';

const CreateUser = () => {
  return (
    <div>
      <Header text="Create New User" />
      <FormInput />
    </div>
  );
};

export default CreateUser;
